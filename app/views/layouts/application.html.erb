<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html>
  <head>
    <title>Gestão Escolar<%= ' - ' + yield(:title) unless yield(:title).blank? %></title>
    <%= stylesheet_link_tag "default" %>
    <%= stylesheet_link_tag "scaffold" %>
    <%= javascript_include_tag :defaults %>
    <%= yield :head %>
  </head>
  <body>
    <h1>Gestão Escolar</h1>
    <%= image_tag("ajax-loader.gif", :id => "loader",
	    :style => "float: right; margin: 7px 15px; background-color: #ff0; display: none") %>
    <ul id="menu">
      <li><%= link_to "Home", root_url %></li>
      <li><%= link_to "Estudantes", students_url %></li>
    </ul>
    <div id="contents">
      <% flash.each do |key, msg| %>
        <%= content_tag :div, msg, :class => "flash_" + key.to_s %>
      <% end %>
      <%= error_messages_for controller.controller_name.singularize %>

      <%= content_tag(:h2, yield(:title)) unless yield(:title).blank? %></h1>

      <%= yield %>
    </div>
  </body>
</html>
